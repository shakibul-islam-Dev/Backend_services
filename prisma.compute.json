{
  "region": "ap-southeast-1",
  "app": {
    "name": "prisma-postgresql",
    "framework": "bun",
    "entry": "src/server.ts",
    "httpPort": 3000,
    "build": {
      "command": "npm run build",
      "outputDirectory": "."
    }
  }
}
